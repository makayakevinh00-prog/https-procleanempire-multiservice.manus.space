/**
 * Zones occupées par l'interface TikTok / Reels par-dessus la vidéo.
 * Rien d'important ne doit s'y trouver, sinon le texte passe sous les boutons
 * "j'aime / commenter / partager" ou sous le pseudo et la description.
 */
export const SAFE = {
  /** Barre de progression et bouton retour. */
  top: 220,
  /** Pseudo, description, bandeau son. */
  bottom: 520,
  /** Colonne d'icônes à droite. */
  right: 220,
  left: 60,
};
